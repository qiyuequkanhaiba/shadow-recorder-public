#[derive(Debug, Clone, Default)]
pub struct RecorderMetrics {
    pub captured_steps_total: u64,
    pub dropped_steps_total: u64,
    pub input_channel_full_drop_total: u64,
    pub capture_queue_drop_total: u64,
    pub encode_queue_drop_total: u64,
    pub push_dispatch_drop_total: u64,
    pub buffer_steps: u32,
    pub buffer_bytes: u64,
    pub last_capture_latency_ms: u32,
    pub last_encode_latency_ms: u32,
    pub stream_backpressure_ms: u32,
    pub last_image_bytes: u32,
    pub current_effective_quality: f32,
    pub quality_adjust_down_count: u32,
    pub quality_adjust_up_count: u32,
    pub wgc_capture_count: u64,
    pub dxgi_capture_count: u64,
    pub effective_input_mode: String,
    pub effective_delta_mode: String,
    pub dirty_rect_supported: bool,
    pub dirty_rect_update_frames_total: u64,
    pub dirty_rect_empty_frames_total: u64,
    pub dirty_rect_encode_skip_total: u64,
    pub dirty_region_frame_total: u64,
    pub dirty_region_empty_frame_total: u64,
    pub dirty_region_coverage_avg: f32,
    pub backend_fallback_total: u64,
    pub capture_context_reset_total: u64,
    pub uia_observer_queue_depth: u32,
    pub uia_observer_dropped_total: u64,
    pub uia_observer_duplicate_drop_total: u64,
    pub uia_observer_rate_limit_drop_total: u64,
    pub uia_observer_queue_overflow_total: u64,
    pub uia_observer_timeout_total: u64,
    pub uia_observer_restart_total: u64,
    pub uia_observer_circuit_open: bool,
    pub uia_observer_polling_attempt_total: u64,
}
